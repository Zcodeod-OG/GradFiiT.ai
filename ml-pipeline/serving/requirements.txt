# GradFiT ML serving container — pinned for reproducible SageMaker builds.
# Torch is installed in the Dockerfile from the cu124 wheel index.

# Web stack (SageMaker BYOC protocol)
flask==3.0.3
gunicorn==23.0.0

# Diffusion + transformers
diffusers==0.32.0
transformers==4.46.3
accelerate==1.1.1
safetensors==0.4.5
peft==0.13.2
sentencepiece==0.2.0
protobuf==4.25.5

# Memory + speed
xformers==0.0.28.post3
bitsandbytes==0.44.1

# SAM2 (Meta) — installed from a pinned commit in scripts/install_sam2.sh
# pulled in via the Dockerfile after pip install -r requirements.txt.

# ControlNet preprocessors
controlnet-aux==0.0.9
opencv-python-headless==4.10.0.84
mediapipe==0.10.20

# IP-Adapter face/style refs
einops==0.8.0
omegaconf==2.3.0

# Real-ESRGAN
basicsr==1.4.2
realesrgan==0.3.0
gfpgan==1.3.8

# I/O
pillow==11.0.0
numpy==1.26.4
boto3==1.35.66
botocore==1.35.66
requests==2.32.3
huggingface-hub==0.26.5

# Logging + perf
psutil==6.1.0
prometheus-client==0.21.1
